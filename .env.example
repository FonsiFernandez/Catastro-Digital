# Catastro Digital — local development example

POSTGRES_DB=cadweb
POSTGRES_USER=cadweb
POSTGRES_PASSWORD=cadweb

WEB_PORT=3000
API_PORT=8000
DB_PORT=5432

# Development only. Replace it in your local .env.
# Production uses .env.production and must use a strong random secret.
JWT_SECRET=CHANGE_ME_FOR_LOCAL_DEVELOPMENT

NEXT_PUBLIC_USE_MOCK_CADASTRE=false
